Instalar las siguientes librerias para que todo funcione:

pip install fastapi
pip install openai
pip install pydantic
pip install uvicorn
pip install PyPDF2

Ademas de instalar node.js y agregarlo a path (todo desde su pagina oficial)
